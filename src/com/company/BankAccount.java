package com.company;


public class BankAccount {
    public BankAccount(String nm, double bal) {
        name = nm;
        balance = bal;

        String name;
        double balance;
    /*public int compareTo(Object Object2)
    {
        BankAccount otherAccount = (BankAccount) Object2;

        if(balance < otherAccount.balance)
        {
            return -1;
        }
        else if(balance > otherAccount.balance)
        {
            return 1;
        }
        else
        {
            return 0;
        }
        //sort by name:
        char fname = name.charAt(0);
        int w = fname;
        char nm = otherAccount.name.charAt(0);
        int e = nm;
        if(w < e)
        {
            return -1;
        }
        else if(w > e)
        {
            return 1;
        }
        else
        {
            return 0;
        }
    }*/



    }
}
